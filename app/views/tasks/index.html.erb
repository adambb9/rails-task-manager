<div class="text-start">
  <h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
</div>
<%= link_to new_task_path do %>
  <div class="btn btn-primary">Create New Task</div>
<% end %>
